// appConfig.ts — центральное место для всего, что связано с версией самого
// приложения FoxFire Hub и адресами, откуда оно берёт данные из интернета.
//
// Если меняешь версию приложения (например, перед новым релизом) — меняй её
// ЗДЕСЬ, а не в разных файлах. Значение ниже должно совпадать с полем
// "version" в package.json и src-tauri/tauri.conf.json — при сборке через
// `npm run tauri build` в заголовке окна и в свойствах установщика будет
// использоваться версия из tauri.conf.json, а внутри интерфейса (например,
// в Настройках и в проверке обновлений) — версия отсюда. Не забывай обновлять
// оба места одновременно.

// Текущая версия приложения. Формат: "МАЖОРНАЯ.МИНОРНАЯ.ПАТЧ" с необязательным
// суффиксом стадии через дефис, например "0.1.0-alpha.1". Суффиксы стадий
// по возрастанию "готовности": alpha -> beta -> rc -> (без суффикса, релиз).
export const APP_VERSION = "0.1.0-alpha.1";

// Понятное для пользователя название текущей стадии — используется для бейджа
// рядом с названием приложения в шапке (см. Header.tsx).
export const APP_STAGE_LABEL = "ALPHA";

// Ссылка на "живой" список виджетов (registry.json). Замени на raw-ссылку
// своего репозитория, когда он будет готов — см. HOW_TO_PUBLISH_REGISTRY.md.
export const REGISTRY_URL = "https://raw.githubusercontent.com/YOUR_USER/YOUR_REGISTRY_REPO/main/registry.json";

// Ссылка на файл с информацией о последней версии САМОГО ПРИЛОЖЕНИЯ (не виджетов).
// Формат файла и как его выложить на GitHub — см. app-version.example.json
// в корне проекта и HOW_TO_PUBLISH_REGISTRY.md.
export const APP_UPDATE_URL = "https://raw.githubusercontent.com/YOUR_USER/YOUR_APP_REPO/main/app-version.json";

// Как часто автоматически проверять обновления приложения, пока оно открыто
// (в миллисекундах). Проверка также всегда происходит один раз сразу при запуске.
export const APP_UPDATE_CHECK_INTERVAL_MS = 6 * 60 * 60 * 1000; // раз в 6 часов
